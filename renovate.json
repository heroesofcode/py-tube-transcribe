{
  "extends": [
    "config:recommended"
  ],
  "packageRules": [
    {
      "matchPackagePatterns": ["*"],
      "automerge": false
    }
  ],
  "commitMessage": {
    "prefix": "deps: ",
    "action": "update",
    "subject": "{{depName}} to v{{newVersion}}"
  }
}
